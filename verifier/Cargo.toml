[workspace]
resolver = "2"
members = ["core", "cli", "gui"]

# `cargo test`/`build` with no --package flag, as the CLAUDE.md commands and CI
# do, runs every member: the CLI, its zero-dependency core, and the GUI alike.

[profile.release]
opt-level = 2
