[project]
name = "commune-polls"
version = "0.1.0"
description = "Self-hosted consultative polling platform for a French commune"
readme = "README.md"
requires-python = ">=3.13,<3.14"
license = "0BSD"
authors = [{ name = "Romain VINCENT" }]
dependencies = [
    "django>=5.2,<6.0",
    "openpyxl>=3.1",
    "argon2-cffi>=23.1",
    "cryptography>=50.0.1",
]

[project.optional-dependencies]
# The WSGI server the Ansible deploy supervises (§15 step 6). An extra, not a
# core dependency: the test suite and the gates never import it, so `uv sync`
# for development stays minimal and the target pulls it with `--extra deploy`.
deploy = ["gunicorn>=23.0"]

[dependency-groups]
dev = [
    "mypy>=1.11",
    "django-stubs[compatible-mypy]>=5.1",
    "ruff>=0.6",
    "pytest>=8.3",
    "pytest-django>=4.9",
]

[tool.ruff]
line-length = 100
target-version = "py313"
src = ["src"]
# docs/manuel/captures/outils/ holds throwaway scripts that seed a demo database
# and render the manual's screen captures. They import Django app internals for
# convenience and are not application code; the four gates do not police them.
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "DJ", "S", "C4", "RUF"]
# The interface, the labels and the comments are French: ambiguous-unicode
# would flag every typographic apostrophe and accented character (§3.8).
ignore = ["S101", "RUF001", "RUF002", "RUF003", "RUF012"]

[tool.ruff.lint.per-file-ignores]
"**/migrations/*" = ["E501", "RUF012"]
"tests/*" = ["S105", "S106", "RUF043"]
"src/config/settings/*" = ["F403", "F405"]

[tool.mypy]
python_version = "3.13"
strict = true
mypy_path = "src"
plugins = ["mypy_django_plugin.main"]
exclude = ["migrations/"]

[[tool.mypy.overrides]]
module = ["openpyxl.*"]
ignore_missing_imports = true

[tool.django-stubs]
django_settings_module = "config.settings.dev"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.test"
pythonpath = ["src"]
testpaths = ["tests"]
python_files = ["test_*.py"]
